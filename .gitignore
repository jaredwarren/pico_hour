# Build output
build/
build-docker/
cmake-build-*/
out/

# Vendored / local Pico SDK checkout (use PICO_SDK_PATH or submodule instead)
pico-sdk/

# Accidental in-tree CMake runs (project Makefile at repo root is not ignored)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
_deps/

# Ninja (if CMake -G Ninja is used outside build/)
.ninja_deps
.ninja_log
rules.ninja

# IDE / language servers
.idea/
*.iml
.cache/
compile_commands.json

# Editor swap / backup
*.swp
*.swo
*~
\#*\#

# OS
.DS_Store
Thumbs.db

# Local overrides (Wi-Fi, paths) — force-add with git add -f if you really want them tracked
include/config.local.h
local.cmake
.env
.env.*

# Logs and temp
*.log
*.tmp
